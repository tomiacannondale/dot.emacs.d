;;========================================================
;; rvm-mode設定
;; https://github.com/senny/rvm.el rvm
;;========================================================
(require 'rvm)
(rvm-use-default)
